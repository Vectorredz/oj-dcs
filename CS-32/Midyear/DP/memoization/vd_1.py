r: int = 5
c: int = 6

matrix: list[list[int]] = [
    [50, 9, -1, 2, 0, 0],
    [ 7, 4, 2, 2, 1, 1],
    [ 9, 6, -1, 99, 1, -1],
    [ 5, 5, 5, -1, 9, 11],
    [ 0, 1, 1, 8, 10, 1]
]

def poutine_path(r: int, c: int, dp: list[list[int]]):
    # for each decision we find the max among them
    # if not the max still count
    
    for i in range(r):
        for j in range(c):
            
    
    
